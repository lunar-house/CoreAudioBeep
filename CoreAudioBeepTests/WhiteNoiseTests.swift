import Testing
import Foundation

@testable import CoreAudioBeep

struct WhiteNoiseTests {
    
    // test that there is a WhiteNoise class that we can instantiate an instance of
    // test that we can ask it for nextSample
    // when we ask it for nextSample, it asks its random number generator for a sample
    
    
}
